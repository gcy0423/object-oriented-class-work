# v0 范围

## 本版本做什么

- 记录当前单体架构。
- 记录当前接口归属。
- 记录当前数据集合。
- 记录当前测试能力。
- 标记文档中“微服务”表述与代码实现的差距。

## 本版本不做什么

- 不改代码。
- 不拆服务。
- 不新增接口。
- 不新增业务模块。
- 不修复生成知识库水分问题。
- 不调整课程设计展示文档。

## 开发约束

后续版本引用 v0 时，必须承认当前是模块化单体。不能把 `AuthService`、`LearningService`、`AITutorService`、`CollaborationService` 称为已独立部署的微服务。

